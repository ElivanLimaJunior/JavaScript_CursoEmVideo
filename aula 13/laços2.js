// vimos primeiro a repetição usando while e o do
// vamos ver agora usando o for


// como exemplo vamos por primeiro um while

/*
var c = 1
while (c<=10) {
    console.log(`${c}`)
    c++
}
*/

//agora vamos usar o for para fazer a mesma coisa que o while está fazendo

for (var c = 1; c <= 10; c++) {
    console.log(`${c}`)
}

/* Distrinchando. 

for (inicio;teste;incremento)


*/