function soma(n1, n2) { // aqui estamos usando dois parametros para relizarmos uma soma.
    return n1 + n2 // aqui pedimos para o return seja o valor de n1 + n2.
}

console.log(soma(2, 5)) // quando vamos passar os parametros aqui, colocamos igual aos de cima separados com uma virgula.